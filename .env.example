# Menshun PAM - Production Environment Configuration
# Copy this to .env and update with your values

# =================================
# Basic Django Configuration
# =================================
DEBUG=False
SECRET_KEY=your-super-secret-key-change-this-value-minimum-50-characters

# =================================
# Field-Level Encryption Key (REQUIRED)
# =================================
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# WARNING: Losing this key means ALL vault passwords become unrecoverable.
#          Back it up securely (e.g., a secrets manager) before deploying.
FIELD_ENCRYPTION_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1,10.0.0.71,yourdomain.com

# =================================
# Database Configuration
# =================================
DATABASE_NAME=menshen_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your-secure-database-password
DATABASE_HOST=db
DATABASE_PORT=5432

# =================================
# Redis Configuration
# =================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# =================================
# Security Settings
# =================================
SECURE_SSL_REDIRECT=false  # Set to true when SSL is configured
SECURE_HSTS_SECONDS=0      # Set to 31536000 when SSL is configured
SESSION_COOKIE_SECURE=false # Set to true when SSL is configured
CSRF_COOKIE_SECURE=false   # Set to true when SSL is configured

# =================================
# Microsoft Entra ID Configuration (Optional)
# =================================
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_REDIRECT_URI=

# Entra Group Mappings
MENSHEN_VAULT_ADMIN_GROUP=Menshen_Vault_Admin
MENSHEN_VAULT_EDITOR_GROUP=Menshen_Vault_Editor
MENSHEN_VAULT_VIEWER_GROUP=Menshen_Vault_Viewer

# =================================
# Microsoft Sentinel Integration (Optional)
# =================================
SENTINEL_ENABLED=false
SENTINEL_WORKSPACE_ID=
SENTINEL_DATA_COLLECTION_ENDPOINT=
SENTINEL_DATA_COLLECTION_RULE_ID=
SENTINEL_STREAM_NAME=Custom-MenshunPAM_CL

# =================================
# Email Configuration (Optional)
# =================================
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
DEFAULT_FROM_EMAIL=noreply@menshun.local

# =================================
# Performance Configuration
# =================================
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
GUNICORN_MAX_REQUESTS=1000
GUNICORN_MAX_REQUESTS_JITTER=100

# =================================
# Logging Configuration
# =================================
LOG_LEVEL=INFO
SENTRY_DSN=